repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.4  # Verwenden Sie die neueste Version
    hooks:
      # Ruff Linter
      - id: ruff
        args: [--fix]
      # Ruff Formatter
      - id: ruff-format

